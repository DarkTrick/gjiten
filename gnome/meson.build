desktop_file = i18n.merge_file(
  input: 'gjiten.desktop.in',
  output: 'gjiten.desktop',
  type: 'desktop',
  po_dir: join_paths(meson.source_root(), 'po'),
  install: true,
  install_dir: join_paths(get_option('datadir'), 'applications')
)